## 1. Layout, badge e identidad por card (fuente `nosotros.css`)

- [x] 1.1 Asegurar `gap: 2.5rem` y `grid 3 col / 1 col` en `.mision_vision__contenido` (`portal/css/nosotros.css:419`) con altura igualada y sin solape, y verificar visualmente en `portal/nosotros/#mision-vision` a 1280px (3 lado a lado) y 375px (1 columna) que gap absorbe hover sin invadir vecina
- [x] 1.2 Añadir badge circular grande `56–64px` para ícono (`fa-flag`/`fa-eye`/`fa-hand-holding-heart`) en cada `.mision_vision__item` en `portal/css/nosotros.css:425` — `width/height 56–64px`, `border-radius:50%`, `background: linear-gradient(135deg, var(--primary), var(--primary-light))` o `var(--gold)` según card, `display:flex; align-items:center; justify-content:center; margin:0 auto 1rem`, ícono `22–28px` centrado `color:#fff`, y verificar `grep -n "mision_vision__badge\|mision_vision__icon" portal/css/nosotros.css | wc -l` ≥ 2 y visual badge arriba del `h2` no pegado al título
- [x] 1.3 Dar acento distinto por card sin salir de paleta `styles.css:4-11` en `portal/css/nosotros.css:425` — p. ej. `.mision_vision__item--mision { border-top:3px solid var(--primary) }`, `--vision { border-top:3px solid var(--gold) }`, `--valores { border-top:3px solid rgba(var(--primary-rgb),0.15) }` o fondo badge diferenciado, usando solo `--primary`/`--gold`/`--primary-rgb`/`--gold-rgb`, y verificar lado a lado que Misión/Visión/Valores se distinguen sin color nuevo fuera de paleta

## 2. Tipografía y hover sutil

- [x] 2.1 Refinar jerarquía `h2`/`p` en `portal/css/nosotros.css:443` — `h2 { font-size: clamp(1.35rem,2vw,1.55rem); font-weight:700–800; color:var(--primary); margin-bottom:1rem }`, `p { line-height:1.85; color:var(--text-muted); max-width:65ch }`, y verificar `grep -n "mision_vision__item h2" portal/css/nosotros.css -A2 | grep "font-size.*1\."` y `p` con `max-width` o `65ch` presente
- [x] 2.2 Pulir hover a `-6px` con sombra y badge en `portal/css/nosotros.css:436` — `transform:translateY(-6px); box-shadow:0 14px 40px rgba(0,0,0,0.08) o var(--shadow-md); border-color: acento de card; .mision_vision__badge { transition: transform 0.3s ease } y :hover .mision_vision__badge { transform:scale(1.05) }`, sin `scale` en card, manteniendo `transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s` y `prefers-reduced-motion` ya existente, y verificar hover no solapa y badge escala sutil
- [x] 2.3 Actualizar HTML `portal/nosotros/index.html:135,139,143` — envolver cada card con `<div class="mision_vision__item mision_vision__item--mision|--vision|--valores"><div class="mision_vision__badge"><i class="fas ..."></i></div><div class="mision_vision__body"><h2>...</h2><p>...</p></div></div>` (badge arriba centrado, `h2` sin `i` inline), manteniendo copy exacto Misión/Visión/Valores, y verificar `grep -c "mision_vision__badge" portal/nosotros/index.html` == 3 y textos idénticos a antes

## 3. Valores en pills, espejo y accesibilidad

- [x] 3.1 Transformar lista Valores a pills/chips en `portal/css/nosotros.css:461` — `ul { display:flex; flex-wrap:wrap; gap:0.5rem; list-style:none }` y `li { display:inline-flex; padding:0.45rem 0.85rem; border:1px solid var(--border); border-radius:var(--radius-pill); background:rgba(var(--primary-rgb),0.05) o #fff; font-size:0.85rem; box-shadow:var(--shadow-sm) }` sin `::before "✦"`, 7 pills con texto completo Responsabilidad…Tolerancia envueltas sin overflow, y verificar `grep -n "valor-pill\|mision_vision__item ul li" portal/css/nosotros.css | head` y visual 7 pills
- [x] 3.2 Espejar todo en `portal/css/mision_vision.css:37-129` — mismo `gap`, `__item` + badge + acentos `--mision/--vision/--valores`, `h2`/`p`, hover `-6px` + badge scale, pills `flex wrap`, breakpoints `min-width:768px 3col` y `prefers-reduced-motion`, eliminando tokens hardcodeados, y verificar `grep -rn "mision_vision__badge\|mision_vision__item--" portal/css --include="*.css" | wc -l` ≥ 4 y `grep -n "#333\|#800000" portal/css/mision_vision.css | wc -l` == 0
- [x] 3.3 Espejar HTML `portal/mision_vision/index.html:59,75,91` con mismos badges/modificadores y pills que `nosotros` (3 badges, 7 pills) y verificar `grep -c "mision_vision__badge" portal/mision_vision/index.html` == 3 y `grep -c "<li>" portal/mision_vision/index.html` == 7 con textos intactos
- [x] 3.4 QA visual `portal/nosotros/#mision-vision` y `portal/mision_vision/` en 1280px/768px/375px (3 col / 1 col, badges 56px centrados, acentos distintos rojo/dorado, títulos 1.5rem, hover -6px + sombra sin solape, Valores 7 pills envueltas, orden/copy intacto, `section-banner`/`#mision-vision` no afectados) y ejecutar `openspec validate --changes --strict` y verificar 0 errores
