## 1. Gap y hover sin solapamiento (fuente: `nosotros.css`)

- [x] 1.1 Aumentar `gap` de `.mision_vision__contenido` en `portal/css/nosotros.css:419` de `2rem` a `2.25rem` o `2.5rem` (desktop y mobile) y verificar visualmente que el hover no invade la card vecina en `portal/nosotros/#mision-vision` a 1280px y 375px
- [x] 1.2 Pulir `.mision_vision__item` en `portal/css/nosotros.css:425` — mantener `background: var(--bg-card)`, `border: 1px solid var(--border)`, `border-radius: var(--radius-lg)`, `box-shadow: 0 4px 20px rgba(0,0,0,0.04)` refinada, `padding: 2rem` (`1.5rem` en ≤768px), y transición limitada a `transform 0.3s ease, box-shadow 0.3s ease, border-color 0.3s ease` (sin `all` ni `scale`), y verificar `grep -n "transition" portal/css/nosotros.css | grep mision_vision` muestra solo esas 3 propiedades
- [x] 1.3 Añadir `position: relative; isolation: isolate;` a `.mision_vision__item` y `z-index: 1` + `transform: translateY(-4px ~ -6px)` + `box-shadow: 0 12px 36px rgba(0,0,0,0.06)` + `border-color: rgba(var(--primary-rgb),0.15)` en `:hover` en `portal/css/nosotros.css:434`, sin `scale`, y verificar que la card en hover queda por encima de sus hermanas sin recorte y que `overflow` del contenedor no recorta la sombra

## 2. Jerarquía tipográfica y lista Valores

- [x] 2.1 Refinar `.mision_vision__item h2` en `portal/css/nosotros.css:439` (`font-size: 1.4rem`, `color: var(--primary)`, `gap: 0.6rem`, `margin-bottom: 1rem`) y `h2 i { color: var(--gold) }` en `448`, y `.mision_vision__item p` en `452` (`line-height: 1.8`, `color: var(--text-muted)`), sin cambiar texto, y verificar que título e ícono contrastan con `p` en inspección visual
- [x] 2.2 Refinar `.mision_vision__item ul`/`li` en `portal/css/nosotros.css:457` — lista sin bullets nativos, `display: grid; grid-template-columns: repeat(auto-fill, minmax(150px,1fr)); gap: 0.5rem`, `li::before { content: "✦"; color: var(--gold) }`, `color: var(--text-muted)`, y verificar que los 7 valores (Responsabilidad…Tolerancia) muestran viñeta dorada alineada sin duplicado

## 3. Alineación página dedicada y accesibilidad

- [x] 3.1 Alinear `portal/css/mision_vision.css:38-92` con `nosotros.css` — mismo `gap`, `__item` (fondo/borde/sombra/padding/transición/hover con `z-index`), `h2`/`p`/`ul li` y breakpoint `@media min-width:768px { grid-template-columns: repeat(3,1fr) }`, eliminando `color: #333` hardcodeado en `38` a favor de `var(--text)`/`var(--text-muted)`, y verificar `grep -rn "mision_vision__item" portal/css --include="*.css"` muestra reglas consistentes en ambos archivos y `grep -n "#333\|#800000" portal/css/mision_vision.css | wc -l` == 0 tras el cambio (o solo donde sea intencional)
- [x] 3.2 Añadir `@media (prefers-reduced-motion: reduce) { .mision_vision__item, .mision_vision__item:hover { transform: none; transition: box-shadow 0.2s ease, border-color 0.2s ease; } }` en `portal/css/nosotros.css` (y espejo en `mision_vision.css`) y verificar con DevTools Rendering → Emulate `prefers-reduced-motion` que el hover no eleva pero sí cambia sombra/borde sin salto
- [x] 3.3 QA visual en `portal/nosotros/#mision-vision` y `portal/mision_vision/` en 1280px/768px/375px (hover cada card, sin solape; sombra no recortada; orden Misión→Visión→Valores intacto; `section-banner` y anclas `#mision-vision` no afectados) y ejecutar `openspec validate --change mejorar-cards-mision-vision-valores --strict` y verificar 0 errores
